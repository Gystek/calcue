#ifndef ARITH_H_
# define ARITH_H_

# include <stdint.h>

int32_t	powi (int32_t, int32_t);
double	pow (double, double);

double	log (double);
double	exp (double);
double	sqrt (double);

#endif /* ARITH_H_ */
